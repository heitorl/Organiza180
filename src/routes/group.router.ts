// import { Router } from "express";
// import { validateSchema } from "../middlewares";
// import { createGroupSchema } from "../schemas/group.schema";
// import groupController from "../controllers/group.controller";
// import validateToken from "../middlewares/validatedToken.middleware";

// const groupRouter = Router()

// groupRouter.post("/create", validateToken, validateSchema(createGroupSchema), groupController.create)
// groupRouter.get("/retrieve", validateToken, groupController.listGroups)

// export default groupRouter
